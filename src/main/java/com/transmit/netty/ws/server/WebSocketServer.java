package com.transmit.netty.ws.server;

import com.transmit.netty.connect.NettyConnection;

public class WebSocketServer extends NettyConnection {

    public void bind(int port){
        super.bind(port);
    }

}
